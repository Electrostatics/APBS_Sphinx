# Example to run a Geoflow calculation.
# Maybe replace foo.pqr with "arg1" or something so that we can pass it in from
# the APBS command line invocation.
#
read_file('foo.pqr')

# file/.pqr
parse_pqr()

# elec/molecular/geoflow
run_geoflow()

write_file('result.txt')
